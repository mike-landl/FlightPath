#pragma once

#include "Mat4.hpp"
#include "ReferenceFrame.hpp"

namespace FlightData
{
    class Solver()
    {
    public:
        Solver();

    private:

    private:

        ReferenceFrame reference_frame_;
    };

}
